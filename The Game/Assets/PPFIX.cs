using UnityEngine;
using System.Collections;
public class PPFIX : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        gameObject.SetActive(false);
        gameObject.SetActive(true);
    }


    
}
